<% include partials/header %>
    <% allBlogs.forEach(function(blog){ %>
        <p>
            <%= blog.title %>
        </p>
        <img src="<%= blog.image %>">
        <p>
            <%= blog.blogContent %>
        </p>
        <p>
            <%= blog.created %>
        </p>
        <%});%>

            <% include partials/footer %>